services:
  protonmail-bridge:
    image: ghcr.io/enucatl/protonmail-bridge:3.23.1
    build:
      context: ./build
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["run"]
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    ports:
      - "10243:1243"
      - "10125:1125"
    volumes:
      - protonmail:/data
      - /opt/docker/traefik/data/certs/docker_key.pem:/protonmail/certs/key.pem:ro
      - /opt/docker/traefik/data/certs/docker_fullchain.pem:/protonmail/certs/cert.pem:ro
    labels:
      - "checkmk_monitor=true"

  protonmail-bridge-init:
    image: ghcr.io/enucatl/protonmail-bridge:3.23.1
    build:
      context: ./build
      dockerfile: Dockerfile
    command: ["init"]
    profiles:
      - init
    stdin_open: true
    tty: true
    volumes:
      - protonmail:/data
      - /opt/docker/traefik/data/certs/docker_key.pem:/protonmail/certs/key.pem:ro
      - /opt/docker/traefik/data/certs/docker_fullchain.pem:/protonmail/certs/cert.pem:ro
volumes:
  protonmail:
